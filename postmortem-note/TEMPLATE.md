# 复盘笔记模板

> 严格使用此模板。`[...]` 为占位符，生成时替换为实际内容。

---

```markdown
---
tags: ['#AI', '#调试', '#配置', '#[...领域标签]']
created: [YYYY-MM-DD]
related:
  - "[[相关笔记1]]"
  - "[[相关笔记2]]"
---

# [一句话标题：什么问题 + 核心教训]

## 📋 问题概述

[2-3 句：在什么环境下、遇到了什么现象、影响范围]

## 🔥 错误演进史

按时间线记录每次修改后的错误变化：

### 第 N 轮：[简短标签]

```
[完整报错信息]
```

**尝试**：[做了什么]
**结果**：[为什么没修好 / 引发了什么新问题]

### 第 N+1 轮：[简短标签]

```
[新报错]
```

**发现**：[在这一轮中找到的关键线索]

> 重复直到最终修复。每轮必须记录**报错原文**（方便未来全文检索）和**为什么没成功**（这是最有价值的部分）。

## 🎯 根因

[不超过 3 句话，直接说出问题的本质]

## ✅ 修复方案

### 修改清单

| # | 文件 | 操作 | 内容 | 原因 |
|---|------|------|------|------|
| 1 | `path/to/file` | 修改/新增/删除 | 具体改了什么 | 为什么这样改 |
| ... | ... | ... | ... | ... |

### 最终配置/代码（关键部分）

```json
// 或对应的代码块
```

### 验证结果

```bash
# 验证命令 + 输出（确认修复生效）
```

## 📐 排查方法论

[如果本次修复体现了通用的排查技巧，记录在此]

```bash
# 可复用的检查命令
command_1
command_2
```

## 🔑 核心教训

1. **[具体规则，不是泛泛而谈]**：例如"JSON 编辑后必须 `python -m json.tool` 验证"而不是"要注意 JSON 格式"
2. **[反直觉发现]**：如果发现了一个与直觉相反的结论
3. **[可复用的检查清单]**：下次遇到类似问题应该先检查什么

## 📎 相关笔记

- [[相关笔记1]] — [简述关系]
- [[相关笔记2]] — [简述关系]
```
